/**
 * Components Barrel Export
 *
 * Provides simplified imports for all components:
 * - import { Lobby, PlayContainer, HostView } from '~/components';
 * - import { Card, Badge } from '~/components';
 */

// Game components
export * from './game';

// UI components
export * from './ui';
